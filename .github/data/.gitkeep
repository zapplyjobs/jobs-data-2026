# Data Directory

This directory contains the output files from the jobs fetcher:
- `jobs-shared.json` - All jobs in JSONL format
- `jobs-metadata.json` - Metadata about the fetch
- `dedupe-store.json` - Global deduplication database
