# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# Environment variables
.env
.env.local

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Temporary files
*.tmp
.tmp/

# Local setup scripts (contain secrets, never commit)
setup-*.sh

# Internal Claude context files — generated locally, must never be committed to public repo
.github/data/ZJP_CONTEXT.md
.github/data/ZJP_SNAPSHOT.json
.github/data/ZJP_OPEN_DECISIONS.md
.github/data/ZJP_DEPENDENCY_GRAPH.md
.github/data/.dep-diagram-hash

# Legacy enriched descriptions — superseded by chunked files (descriptions-enriched-1.jsonl, -2.jsonl, ...)
# INF-7: file was split in S242. Untracked to prevent re-tracking via git add -u.
.github/data/descriptions-enriched.jsonl